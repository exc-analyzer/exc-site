[extend]
useDefault = true

[allowlist]
description = "Build output, dependencies and local environment files"
paths = [
  '''app/node_modules/.*''',
  '''app/dist/.*''',
  '''(^|/)\.env\.example$''',
]
